# Benchmarks

This directory stores benchmark inputs, manifests, and expected metadata used by the harness.

Initial scope:

- `datasets/smoke/images/`: placeholder location for small smoke benchmark inputs
- `manifests/smoke_manifest.json`: a lightweight benchmark manifest for harness wiring
- `golden/`: reserved for approved comparison references

Keep runtime assets and benchmark assets separated.
