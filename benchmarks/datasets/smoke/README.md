# Smoke Benchmark Dataset

Place a small set of stable smoke images in `images/`.

Expected use:

- quick harness validation
- pipeline smoke execution
- baseline vs candidate wiring checks

Do not place large experiment outputs here.
